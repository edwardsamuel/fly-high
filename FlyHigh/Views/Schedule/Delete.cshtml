@model FlyHigh.Models.Schedule

@{
    ViewBag.Title = "Delete";
}

<fieldset>
    <legend>Delete Schedule</legend>
    <h2>Are you sure you want to delete this?</h2>

    <div class="row">
        <div class="col-md-2">
            <b>@Html.DisplayNameFor(model => model.Flight.FlightId)</b>
        </div>
        <div class="col-md-8">
            @Html.DisplayFor(model => model.Flight.FlightId)
        </div>
    </div>

    <div class="row">
        <div class="col-md-2">
            <b>@Html.DisplayNameFor(model => model.Date)</b>
        </div>
        <div class="col-md-8">
            @Html.DisplayFor(model => model.Date)
        </div>
    </div>


    <div class="row">
        <div class="col-md-2">
            <b>@Html.DisplayNameFor(model => model.Plane.PlaneType)</b>
        </div>
        <div class="col-md-8">
            @Html.DisplayFor(model => model.Plane.PlaneType)
        </div>
    </div>

</fieldset>
@using (Html.BeginForm())
{
    <p>
        <div class="row"></div>
        <input class="btn btn-danger btn-sm" type="submit" value="Delete" />
        @Html.ActionLink("Back to List", "Index", "Schedule", new { @class = "btn btn-default btn-sm" })
    </p>
}
