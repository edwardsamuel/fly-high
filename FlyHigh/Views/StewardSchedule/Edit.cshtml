@model FlyHigh.Models.StewardSchedule

@{
    ViewBag.Title = "Edit";
}

<h1>Steward Schedule</h1>

@using (Html.BeginForm("Edit", "StewardSchedule", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Edit Steward Schedule</legend>

        @Html.HiddenFor(model => model.StewardScheduleId)

        <div class="form-group">
            @Html.LabelFor(model => model.StewardId, "Steward", new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.DropDownList("StewardId", ViewBag.StewardName as SelectList, new { @class = "form-control input-sm" })
                @Html.ValidationMessageFor(model => model.StewardId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ScheduleId, "Schedule Id", new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.DropDownList("ScheduleId", ViewBag.ScheduleId as SelectList, new { @class = "form-control input-sm", onchange = "FetchPilots();" })
                @Html.ValidationMessageFor(model => model.ScheduleId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IsHead, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                <div class="checkbox">
                    @Html.EditorFor(model => model.IsHead, new { @class = "form-control input-sm" })
                    @Html.ValidationMessageFor(model => model.IsHead)
                </div>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input class="btn btn-success btn-sm" type="submit" value="Save">
                @Html.ActionLink("Back to List", "Index", "StewardSchedule", new { @class = "btn btn-default btn-sm" })
            </div>
        </div>
    </fieldset>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
